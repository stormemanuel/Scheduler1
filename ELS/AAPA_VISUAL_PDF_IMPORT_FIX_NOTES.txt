AAPA visual PDF import fix

This build keeps the existing event import feature and patches the PDF reader for table text that extracts out of visual order.

Fixes:
- Handles PDF rows where the time/role/phone line appears above the date/name line.
- Restores missing AAPA rows such as Stephen Saia and Sam Schexnaydre on May 15.
- Keeps adjacent booth headers mapped to the correct row block:
  - AXSOME -> Stephen Saia
  - CORCEPT -> Kevin Muphey/Kevin Murphy
  - ESPERION -> Sam Schexnaydre
  - GENENTECH -> Brendan Hassett
- Preserves the existing conflict rule: the same crew member cannot be assigned to overlapping calls on the same date.
- Keeps prior VS Exchange page-break fixes, OPTOS detection, role aliases, payout logic, booth/day view selector, and inline edit behavior.

Validation performed:
- TypeScript check: npx tsc --noEmit passed.
- Local parser test against the visually extracted AAPA PDF text returned 9 crew rows, 3 labor days, and 9 sub-calls.
- Local parser test against the visually extracted VS Exchange PDF text returned 18 crew rows, 4 labor days, 9 sub-calls, and OPTOS Booth 923.

Upload note:
- package.json is at the repository root, so Vercel Root Directory should be blank.
