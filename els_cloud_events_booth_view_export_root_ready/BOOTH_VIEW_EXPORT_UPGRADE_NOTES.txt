ELS Events booth view / export upgrade

What changed:
- Added a Change view selector on the selected Event card.
- Existing day/sub-call event view is preserved.
- New Booth / area view groups the same imported event data by booth or area first, then date, then calls.
- Export Crew List now follows the selected view:
  - Day view export includes stronger day separators.
  - Booth view export is formatted like the uploaded call lists: booth/area header, date sections, then crew rows.
- Existing PDF/CSV import flow was not removed or replaced.
- Existing crew assignment, rate alias, and payout fixes are preserved.

Upload notes:
- This is root-ready. package.json is at the top level.
- In Vercel, Root Directory should stay blank.
